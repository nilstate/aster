{
  "name": "automaton",
  "private": true,
  "version": "0.1.0",
  "description": "Governed dogfood destination for runx live runs.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "check": "node scripts/check.mjs",
    "docs:build": "npx -y sourcey build --config docs/sourcey.config.ts -o .sourcey/runx-docs",
    "docs:ci": "npm run check && npm run docs:build"
  }
}
